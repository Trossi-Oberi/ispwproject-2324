package logic.dao;

public class GroupDAO {
    //TODO: gestione gruppo già esistente per un evento, gestione nome gruppo già preso, uscita utente dal gruppo, creazione gruppo, eliminazione gruppo (isOwner booleano)
    //TODO: aggiungere nel DB Groups table con group_id (unique primary key autoincrement), group_name, event_id (unique foreign key), group_owner (int id dell'owner), user_id


}
